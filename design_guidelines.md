# 心动助手 设计指南

## 1. 设计理念

**核心逻辑**：多对象管理 → 对象档案 → AI建议 → 推进关系 → 获取信息 → 循环

**信息框架**：了解一个人 = 了解硬件 + 了解软件

**设计原则**：
- 简洁至上：移除所有非必要元素
- 流程驱动：每个页面引导用户完成一个核心动作
- 信息聚焦：突出关键信息，弱化次要信息

---

## 2. 信息架构（硬件/软件框架）

### 硬件信息（外在/固定属性）
硬件信息是**可见的、相对固定的**外在属性，通常可以直接观察或简单询问获得。

| 分类 | 字段 | 说明 |
|------|------|------|
| **基本信息** | 年龄、身高、生日、星座、血型 | 基础人口学信息 |
| **外貌特征** | 体型、穿搭风格 | 外观相关属性 |
| **联系方式** | 微信、电话、所在地 | 联络渠道 |
| **职业信息** | 职业、公司、职位 | 社会身份 |

### 软件信息（内在/需探索属性）
软件信息是**内在的、需要互动探索**的特质，需要通过交流和相处逐步了解。

| 分类 | 字段 | 说明 |
|------|------|------|
| **性格特质** | MBTI、性格描述、情绪特点 | 心理特征 |
| **兴趣爱好** | 兴趣标签、具体爱好 | 休闲偏好 |
| **行为习惯** | 作息、消费观、沟通风格 | 日常模式 |
| **情感需求** | 喜欢/讨厌、恋爱期待、雷区 | 情感偏好 |

---

## 3. 配色方案（黑白灰）

### 基础色

| 用途 | 颜色值 | Tailwind 类名 |
|------|--------|---------------|
| 主色（强调） | `#000000` | `text-black` / `bg-black` |
| 标题文字 | `#111827` | `text-gray-900` |
| 正文文字 | `#374151` | `text-gray-700` |
| 辅助文字 | `#6B7280` | `text-gray-500` |
| 占位文字 | `#9CA3AF` | `text-gray-400` |
| 边框/分割 | `#E5E7EB` | `border-gray-200` |
| 背景色 | `#F9FAFB` | `bg-gray-50` |
| 卡片背景 | `#FFFFFF` | `bg-white` |

### 状态色（仅用于必要的状态标识）

| 状态 | 颜色 | Tailwind 类名 | 使用场景 |
|------|------|---------------|----------|
| 成功/完成 | `#10B981` | `text-emerald-600` | 任务完成、关系进展 |
| 警示 | `#F59E0B` | `text-amber-600` | 重要提醒 |
| 数量标记 | `#6366F1` | `text-indigo-500` | 数字、计数 |

---

## 4. 字体规范

| 层级 | 字号 | 字重 | Tailwind 类名 |
|------|------|------|---------------|
| 页面标题 | 20px | Bold | `text-xl font-bold` |
| 卡片标题 | 16px | Semibold | `text-base font-semibold` |
| 正文 | 14px | Regular | `text-sm` |
| 辅助文字 | 12px | Regular | `text-xs` |

---

## 5. 间距系统

| 用途 | 间距 | Tailwind 类名 |
|------|------|---------------|
| 页面边距 | 16px | `px-4` |
| 卡片间距 | 12px | `mb-3` |
| 内容间距 | 8px | `gap-2` |
| 紧凑间距 | 4px | `gap-1` |

---

## 6. 组件规范

### 卡片
```
bg-white rounded-xl border border-gray-100
```

### 按钮
```
主按钮：bg-black text-white
次按钮：bg-gray-100 text-gray-700
文字按钮：text-gray-500
```

### 标签
```
bg-gray-100 text-gray-600 text-xs
```

### 信息分组标题
```
<View className="flex items-center gap-2 mb-2">
  <HardDrive size={14} color="#6B7280" />
  <Text className="block text-xs text-gray-400">硬件信息</Text>
  <Text className="block text-xs text-gray-300">外在属性</Text>
</View>
```

---

## 7. 页面结构

### 首页（对象列表）
- 顶部：标题 + 新建按钮
- 主体：对象卡片列表（姓名、年龄、职业、关系阶段）
- 底部：TabBar

### 对象详情页
- 顶部：姓名 + 年龄 + 职业 + MBTI/星座标签
- 基本卡片：关系阶段 + 互动状态
- 硬件信息区：基本信息、职业信息、联系方式、外貌特征
- 软件信息区：性格特质、兴趣爱好、行为习惯、情感需求
- 操作区：任务入口、约会记录入口
- AI建议区：获取下一步建议
- 底部：主要行动按钮

### 编辑/创建页
- 姓名（必填）
- 硬件信息区（分组折叠或平铺）
- 软件信息区（分组折叠或平铺）
- 关系状态区
- 底部：保存按钮

---

## 8. 交互原则

1. **一页一事**：每个页面只做一件事
2. **减少选择**：提供明确的行动路径
3. **即时反馈**：操作后立即显示结果
4. **渐进展示**：先看概览，再深入详情
5. **信息分层**：硬件在前，软件在后（外在信息更容易获取）

---

## 9. 导航结构

| TabBar | 页面 | 核心功能 |
|--------|------|----------|
| 对象 | 首页 | 多对象管理入口 |
| 发现 | 发现页 | 功能扩展（预留） |
| 我的 | 个人页 | 设置与数据 |
